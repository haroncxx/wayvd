#!/usr/bin/env bash
set -euo pipefail

# Keep the GUI source editable in the repository while exposing a stable
# `wayvd-ui` command from the same directory.
exec "$(dirname "$(readlink -f -- "$0")")/wayvd-ui.py" "$@"
